from pathlib import Path

root = Path('buildsrc/lan_file_transfer_v1/android')
main = root / 'app/src/main/java/ir/local/lantransfer/MainActivity.java'
gradle = root / 'app/build.gradle.kts'
text = main.read_text(encoding='utf-8')

def rep(old, new):
    global text
    if old not in text:
        raise SystemExit('Expected block not found:\n' + old[:240])
    text = text.replace(old, new, 1)

rep('''        root.addView(buildTopBar(), new LinearLayout.LayoutParams(\n                LinearLayout.LayoutParams.MATCH_PARENT, dp(66)));''', '''        root.addView(buildTopBar(), new LinearLayout.LayoutParams(\n                LinearLayout.LayoutParams.MATCH_PARENT, dp(isCompactWidth() ? 56 : 62)));''')
rep('''        root.addView(buildBottomNavigation(), new LinearLayout.LayoutParams(\n                LinearLayout.LayoutParams.MATCH_PARENT, dp(68)));''', '''        root.addView(buildBottomNavigation(), new LinearLayout.LayoutParams(\n                LinearLayout.LayoutParams.MATCH_PARENT, dp(isCompactWidth() ? 60 : 64)));''')
rep('''        bar.setPadding(dp(16), 0, dp(16), 0);''', '''        bar.setPadding(dp(isCompactWidth() ? 12 : 16), 0, dp(isCompactWidth() ? 12 : 16), 0);''')
rep('''        LinearLayout.LayoutParams logoLp = new LinearLayout.LayoutParams(dp(38), dp(38));''', '''        LinearLayout.LayoutParams logoLp = new LinearLayout.LayoutParams(dp(isCompactWidth() ? 32 : 36), dp(isCompactWidth() ? 32 : 36));''')
rep('''        TextView brand = text("LAN SHARE", 19, Color.WHITE, true);\n        TextView sub = text("انتقال فایل در Wi‑Fi محلی", 10, Color.argb(220,255,255,255), false);''', '''        TextView brand = text("LAN SHARE", isCompactWidth() ? 16 : 18, Color.WHITE, true);\n        brand.setMaxLines(1);\n        TextView sub = text("انتقال فایل در Wi‑Fi محلی", isCompactWidth() ? 8 : 9, Color.argb(220,255,255,255), false);\n        sub.setMaxLines(1);''')
rep('''        topStatusText.setBackground(roundRect(Color.argb(38,255,255,255), dp(20)));\n        bar.addView(topStatusText);''', '''        topStatusText.setBackground(roundRect(Color.argb(38,255,255,255), dp(20)));\n        if (isCompactWidth()) topStatusText.setVisibility(View.GONE);\n        bar.addView(topStatusText);''')
rep('''        item.setPadding(dp(4), dp(6), dp(4), dp(4));''', '''        item.setPadding(dp(3), dp(4), dp(3), dp(3));''')
rep('''        item.addView(icon, new LinearLayout.LayoutParams(dp(24), dp(24)));\n        TextView text = text(label, 10, DesignTokens.TEXT_SECONDARY, false);''', '''        item.addView(icon, new LinearLayout.LayoutParams(dp(isCompactWidth() ? 21 : 23), dp(isCompactWidth() ? 21 : 23)));\n        TextView text = text(label, isCompactWidth() ? 8 : 9, DesignTokens.TEXT_SECONDARY, false);\n        text.setMaxLines(1);''')
rep('''        TextView pageTitle = text("انتقال فایل", 23, DesignTokens.TEXT_PRIMARY, true);''', '''        TextView pageTitle = text("انتقال فایل", isCompactWidth() ? 20 : 22, DesignTokens.TEXT_PRIMARY, true);''')
rep('''        content.addView(description, matchBottom(18));''', '''        content.addView(description, matchBottom(isCompactWidth() ? 12 : 16));''')
rep('''        quick.setOrientation(LinearLayout.HORIZONTAL);''', '''        quick.setOrientation(isVeryCompactWidth() ? LinearLayout.VERTICAL : LinearLayout.HORIZONTAL);''')
rep('''        content.addView(quick, matchBottom(16));\n        quick.addView(actionTile(R.drawable.ic_send, "ارسال فایل", "انتخاب چند فایل", v -> pickFiles()), weightedTile(8));\n        quick.addView(actionTile(R.drawable.ic_folder, "دریافتی‌ها", "مشاهده فایل‌ها", v -> showPage(PAGE_RECEIVED)), weightedTile(0));''', '''        content.addView(quick, matchBottom(isCompactWidth() ? 10 : 14));\n        View sendTile = actionTile(R.drawable.ic_send, "ارسال فایل", "انتخاب چند فایل", v -> pickFiles());\n        View receivedTile = actionTile(R.drawable.ic_folder, "دریافتی‌ها", "مشاهده فایل‌ها", v -> showPage(PAGE_RECEIVED));\n        if (isVeryCompactWidth()) {\n            quick.addView(sendTile, matchBottom(8));\n            quick.addView(receivedTile, match());\n        } else {\n            quick.addView(sendTile, weightedTile(8));\n            quick.addView(receivedTile, weightedTile(0));\n        }''')
rep('''        connection.setPadding(dp(16), dp(16), dp(16), dp(16));''', '''        connection.setPadding(dp(isCompactWidth() ? 13 : 16), dp(isCompactWidth() ? 13 : 16),\n                dp(isCompactWidth() ? 13 : 16), dp(isCompactWidth() ? 13 : 16));''')
rep('''            ImageView radarIcon = iconView(R.drawable.ic_radar, DesignTokens.PRIMARY, "Radar");\n            radarRow.addView(radarIcon, new LinearLayout.LayoutParams(dp(34), dp(34)));\n            TextView radarHelp = text("Radar دستگاه‌های LAN SHARE روی همین Wi‑Fi را پیدا می‌کند.", 10,\n                    DesignTokens.TEXT_SECONDARY, false);\n            radarHelp.setPadding(dp(10), 0, 0, 0);\n            radarRow.addView(radarHelp, new LinearLayout.LayoutParams(0, LinearLayout.LayoutParams.WRAP_CONTENT, 1));\n            Button radar = secondaryButton("جست‌وجو");\n            radar.setOnClickListener(v -> discoverPc(false));\n            radarRow.addView(radar);\n            connection.addView(radarRow, matchBottom(12));\n\n            pinField = input("PIN شش‌رقمی روی کامپیوتر", true);\n            connection.addView(pinField, fixedBottom(52, 10));\n            Button pair = primaryButton("اتصال امن");\n            pair.setOnClickListener(v -> pairAndConnect());\n            connection.addView(pair, fixedBottom(52, 12));\n\n            TextView manual = text("اگر Radar نتیجه نداد، IP کامپیوتر را دستی وارد کنید.", 9,\n                    DesignTokens.TEXT_SECONDARY, false);\n            connection.addView(manual, matchBottom(6));\n            serverField = input("192.168.1.10:8765", false);\n            if (!currentServer.isEmpty()) serverField.setText(currentServer);\n            connection.addView(serverField, fixedBottom(52, 0));''', '''            ImageView radarIcon = iconView(R.drawable.ic_radar, DesignTokens.PRIMARY, "Radar");\n            radarIcon.setVisibility(isCompactWidth() ? View.GONE : View.VISIBLE);\n            radarRow.addView(radarIcon, new LinearLayout.LayoutParams(isCompactWidth() ? 0 : dp(30), isCompactWidth() ? 0 : dp(30)));\n            TextView radarHelp = text("جست‌وجوی کامپیوتر روی همین Wi‑Fi", isCompactWidth() ? 9 : 10,\n                    DesignTokens.TEXT_SECONDARY, false);\n            radarHelp.setPadding(isCompactWidth() ? 0 : dp(8), 0, 0, 0);\n            radarRow.addView(radarHelp, new LinearLayout.LayoutParams(0, LinearLayout.LayoutParams.WRAP_CONTENT, 1));\n            Button radar = secondaryButton("جست‌وجو");\n            radar.setOnClickListener(v -> discoverPc(false));\n            radarRow.addView(radar, new LinearLayout.LayoutParams(dp(isCompactWidth() ? 92 : 104), dp(44)));\n            connection.addView(radarRow, matchBottom(10));\n\n            pinField = input("PIN شش‌رقمی روی کامپیوتر", true);\n            connection.addView(pinField, fixedBottom(isCompactWidth() ? 48 : 52, 8));\n            Button pair = primaryButton("اتصال امن");\n            pair.setOnClickListener(v -> pairAndConnect());\n            connection.addView(pair, fixedBottom(isCompactWidth() ? 48 : 52, 8));\n\n            serverField = input("192.168.1.10:8765", false);\n            if (!currentServer.isEmpty()) serverField.setText(currentServer);\n            serverField.setVisibility(View.GONE);\n            Button manualToggle = ghostButton("ورود IP دستی");\n            manualToggle.setOnClickListener(v -> {\n                boolean opening = serverField.getVisibility() != View.VISIBLE;\n                serverField.setVisibility(opening ? View.VISIBLE : View.GONE);\n                manualToggle.setText(opening ? "بستن اتصال دستی" : "ورود IP دستی");\n            });\n            connection.addView(manualToggle, fixedBottom(44, 6));\n            connection.addView(serverField, fixedBottom(48, 0));''')
rep('''        TextView recentTitle = text("آخرین فعالیت", 15, DesignTokens.TEXT_PRIMARY, true);\n        content.addView(recentTitle, matchBottom(8));\n        recentTransferContainer = new LinearLayout(this);\n        recentTransferContainer.setOrientation(LinearLayout.VERTICAL);\n        content.addView(recentTransferContainer, match());\n        renderRecentTransfers();''', '''        if (!isCompactWidth()) {\n            TextView recentTitle = text("آخرین فعالیت", 15, DesignTokens.TEXT_PRIMARY, true);\n            content.addView(recentTitle, matchBottom(8));\n            recentTransferContainer = new LinearLayout(this);\n            recentTransferContainer.setOrientation(LinearLayout.VERTICAL);\n            content.addView(recentTransferContainer, match());\n            renderRecentTransfers();\n        } else {\n            TextView hint = text("جزئیات انتقال‌ها در تب «انتقال‌ها» نمایش داده می‌شود.", 9,\n                    DesignTokens.TEXT_SECONDARY, false);\n            hint.setGravity(Gravity.CENTER);\n            content.addView(hint, match());\n        }''')
rep('''        tile.setPadding(dp(14), dp(14), dp(14), dp(14));''', '''        tile.setPadding(dp(isCompactWidth() ? 10 : 13), dp(isCompactWidth() ? 10 : 13),\n                dp(isCompactWidth() ? 10 : 13), dp(isCompactWidth() ? 10 : 13));''')
rep('''        tile.addView(icon, new LinearLayout.LayoutParams(dp(34), dp(34)));\n        TextView t = text(title, 13, DesignTokens.TEXT_PRIMARY, true);''', '''        tile.addView(icon, new LinearLayout.LayoutParams(dp(isCompactWidth() ? 27 : 32), dp(isCompactWidth() ? 27 : 32)));\n        TextView t = text(title, isCompactWidth() ? 11 : 12, DesignTokens.TEXT_PRIMARY, true);''')
rep('''        TextView s = text(subtitle, 9, DesignTokens.TEXT_SECONDARY, false);''', '''        TextView s = text(subtitle, isCompactWidth() ? 8 : 9, DesignTokens.TEXT_SECONDARY, false);''')
rep('''        content.setPadding(dp(16), dp(18), dp(16), dp(24));''', '''        int side = isCompactWidth() ? 12 : 16;\n        content.setPadding(dp(side), dp(isCompactWidth() ? 12 : 16), dp(side), dp(isCompactWidth() ? 16 : 22));''')
rep('''    private LinearLayout.LayoutParams weightedTile(int trailingDp) {\n        LinearLayout.LayoutParams lp = new LinearLayout.LayoutParams(0, dp(122), 1);''', '''    private LinearLayout.LayoutParams weightedTile(int trailingDp) {\n        LinearLayout.LayoutParams lp = new LinearLayout.LayoutParams(0, dp(isCompactWidth() ? 94 : 110), 1);''')
rep('''    private int dp(int value) {\n        return Math.round(value * getResources().getDisplayMetrics().density);\n    }''', '''    private boolean isCompactWidth() {\n        return getResources().getConfiguration().screenWidthDp < 400;\n    }\n\n    private boolean isVeryCompactWidth() {\n        return getResources().getConfiguration().screenWidthDp < 350;\n    }\n\n    private int dp(int value) {\n        return Math.round(value * getResources().getDisplayMetrics().density);\n    }''')

main.write_text(text, encoding='utf-8')

g = gradle.read_text(encoding='utf-8')
g = g.replace('versionCode = 4', 'versionCode = 5').replace('versionName = "2.2.0"', 'versionName = "2.3.0"')
gradle.write_text(g, encoding='utf-8')
print('Applied responsive Android V2.3 patch')
